#!/bin/bash

dotfiles_dir="$HOME/.dotfiles"
dotfiles_dirty_file="$HOME/.dotfiles_dirty"
dotfiles_password_file="$HOME/.dotfiles_password"
dotfiles_checksum_file="$dotfiles_dir/.dotfiles_sum"
dotfiles_lib_file="$dotfiles_dir/.dotfiles_lib"
gitrepo_ssh="git@github.com:bjornhusberg"
gitrepo_https="https://github.com/bjornhusberg"

function run() {
  command="$1"
  parameter="$2"
  rm -f $dotfiles_dirty_file
  if [[ ! -d "$dotfiles_dir" ]]; then
    init
  elif [[ "$command" == "reset" ]]; then
    reset
  elif [[ "$command" == "pull" ]]; then
    pull
  elif [[ "$command" == "push" ]]; then
    push "$parameter"
  elif [[ "$command" == "add" ]]; then
    add "$parameter"
  elif [[ "$command" == "rm" ]]; then
    remove "$parameter"
  elif [[ "$command" == "checksum" ]]; then
    checksum
  elif [[ "$command" == "diff" ]]; then
    diffFiles
  elif [[ "$command" == "" ]]; then
    status
  else
    echo "Usage: $0 [diff|pull|push|add|rm|reset|checksum] files ..."
    exit 1
  fi
}

function initGitrepo() {
  sshResult="$(ssh -qT git@github.com 2>&1)"
  if [[ "${sshResult:0:2}" == "Hi" ]]; then
    gitrepo="${gitrepo_ssh}"
  else
    gitrepo="${gitrepo_https}"
  fi
}

function init() {
  initGitrepo
  url="${gitrepo}/dotfiles.git"
  echo "Initializing dotfiles from $url."
  mkdir -p "$dotfiles_dir"
  pushd "$dotfiles_dir" > /dev/null
  git clone -q "$url" .
  if [[ -f "$dotfiles_lib_file" ]]; then
    mkdir lib
    pushd "lib" > /dev/null
    for lib in $(cat "$dotfiles_lib_file"); do
      url="${gitrepo}/${lib}.git"
      echo "Initializing $lib from $url."
      git clone -q "$url"
    done
    popd > /dev/null
  fi
  popd > /dev/null
  updateFiles
}

function reset() {
  echo "Resetting dotfiles."
  repositoryPull
  diffFiles
  updateFiles
}

function pull() {
  echo "Pulling dotfiles."
  diffFiles
  if (( $? != 0 )); then
    echo "Dotfiles contain local changes."
    exit
  fi
  readPassword "Enter password for data verification (or leave blank to skip)"
  if [[ -z "$password" ]]; then
    repositoryPull
  else
    verifyChecksum
    repositoryPull
    verifyChecksum
  fi
  diffFiles
  updateFiles
}

function push() {
  echo "Pushing dotfiles."
  path="$1"
  diffFiles
  if (( $? == 0 )); then
    echo "Nothing to do."
    exit
  fi
  repositoryCheck
  if (( $? != 0 )); then
    echo "Dotfiles need to be merged."
    exit
  fi
  readPassword
  verifyChecksum
  commitFiles
  updateChecksum
  repositoryPush "$path"
}

function add() {
  path="$1"
  repositoryCheck
  if (( $? != 0 )); then
    echo "Dotfiles need to be pulled."
    exit
  fi
  echo "Adding dotfile $path."
  readPassword
  verifyChecksum
  addFile "$path"
  updateChecksum
  repositoryPush "Added $path."
}

function remove() {
  path="$1"
  repositoryCheck
  if (( $? != 0 )); then
    echo "Dotfiles need to be pulled."
    exit
  fi
  echo "Removing dotfile $path."
  readPassword
  verifyChecksum
  deleteFile "$path"
  updateChecksum
  repositoryPush "Removed $path."
}

function checksum() {
  echo "Updating dotfiles checksum."
  readPassword
  updateChecksum
}

function status() {
  diffFiles > /dev/null 2>&1
  if (( $? != 0 )); then
    echo "Dotfiles need to be pushed."
    touch $dotfiles_dirty_file
    exit
  fi
  repositoryCheck
  if (( $? != 0 )); then
    echo "Dotfiles need to be pulled."
    touch $dotfiles_dirty_file
    exit
  fi
}

function repositoryPull() {
  directoryPull "$dotfiles_dir"
  if [[ -f "$dotfiles_lib_file" ]]; then
    for lib in $(cat "$dotfiles_lib_file"); do
      directoryPull "$dotfiles_dir/lib/$lib"
    done
  fi
}

function directoryPull() {
  directory="$1"
  pushd "$directory" > /dev/null
  git clean -q -d -x -f
  git checkout -q -- .
  git pull -q origin master
  git clean -q -d -x -f
  popd > /dev/null
}

function repositoryCheck() {
  pushd "$dotfiles_dir" > /dev/null
  git remote update > /dev/null 2>&1
  status=$(git status --porcelain --branch | head -n 1 | grep behind)
  popd > /dev/null
  if [[ -z "$status" ]]; then
    return 0
  else
    return 1
  fi
}

function repositoryPush() {
  comment="$1"
  if [[ -z "$comment" ]]; then
    comment="Updated from $HOSTNAME"
  fi
  directoryPush "$dotfiles_dir" "$comment"
  if [[ -f "$dotfiles_lib_file" ]]; then
    for lib in $(cat "$dotfiles_lib_file"); do
      directoryPush "$dotfiles_dir/lib/$lib" "$comment"
    done
  fi
}

function directoryPush() {
  directory="$1"
  comment="$2"
  pushd "$directory" > /dev/null
  git add -A .
  status=$(git status --porcelain)
  if [[ ! -z "$status" ]]; then
    git commit -q -m "$comment"
    git push -q origin master
  fi
  popd > /dev/null
}

function isUpdated() {
  path="$1"
  if [[ ! -f "$HOME$path" ]]; then
    return 1
  elif [[ "$HOME$path" -ot "$dotfiles_dir$path"
       || "$HOME$path" -nt "$dotfiles_dir$path" ]]; then
    return 1
  fi
  return 0
}

function diffFiles() {
  result=$(
    find "$dotfiles_dir" ! -path "*/.git*" -type f ! -name ".DS_Store" ! -name ".dotfiles_sum" ! -name "README.md" -print0 | sort -z | {
      while read -d $'\0' file; do
        file="${file:${#dotfiles_dir}}"
        isUpdated "$file"
        if (( $? == 0 )); then
          continue
        fi
        diffFile "$file" >&2
        if (( $? != 0 )); then
          echo "1"
        fi
      done
    }
  )
  if [[ ! -z "$result" ]]; then
    return 1
  else
    return 0
  fi
}

function diffFile() {
  if [[ ! -f "$HOME$file" ]]; then
    echo "MISSING: $file"
    return 1
  else
    diff=$(diff "$HOME$file" "$dotfiles_dir$file")
    if [[ ! -z "$diff" ]]; then
      echo "MODIFIED: $HOME$file"
      echo "$diff"
      return 1
    fi
  fi
  return 0
}

function addFile() {
  file=$(abspath "$1")
  if [[ "${file:0:${#HOME}}" != "$HOME" ]]; then
    echo "File not in home directory: $file"
    return
  fi
  if [[ ! -f "$file" ]]; then
    echo "File not found: $file"
    return
  fi
  file="${file:(${#HOME}+1)}"
  if [[ -f "$dotfiles_dir/$file" ]]; then
    echo "File already exists: $file"
    return
  fi
  echo "Adding $file"
  directory=$(dirname "$dotfiles_dir/$file")
  mkdir -p "$directory" 2> /dev/null
  cp -p "$HOME/$file" "$dotfiles_dir/$file"
}

function deleteFile() {
  file=$(abspath "$1")
  if [[ "${file:0:${#HOME}}" != "$HOME" ]]; then
    echo "File not in home directory: $file"
    return
  fi
  file="${file:(${#HOME}+1)}"
  if [[ ! -f "$dotfiles_dir/$file" ]]; then
    echo "File not found: $file"
    return
  fi
  echo "Deleting $file"
  rm "$dotfiles_dir/$file"
}

function commitFiles() {
  find "$dotfiles_dir" ! -path "*/.git*" -type f ! -name ".DS_Store" ! -name ".dotfiles_sum" ! -name "README.md" -print0 | sort -z | while read -d $'\0' file; do
    file="${file:${#dotfiles_dir}}"
    isUpdated "$file"
    if (( $? != 0 )); then
      echo "Committing file $file"
      cp -p "$HOME$file" "$dotfiles_dir$file"
    fi
  done
}

function updateFiles() {
  find "$dotfiles_dir" ! -path "*/.git*" -type f ! -name ".DS_Store" ! -name ".dotfiles_sum" ! -name "README.md" -print0 | sort -z | while read -d $'\0' file; do
    file="${file:${#dotfiles_dir}}"
    isUpdated "$file"
    if (( $? != 0 )); then
      echo "Updating $file"
      rm -f "$HOME$file"
      directory=$(dirname "$HOME$file")
      mkdir -p "$directory" 2> /dev/null
      cp -p "$dotfiles_dir$file" "$HOME$file"
    fi
  done
}

function readPassword() {
  if [[ -f "$dotfiles_password_file" ]]; then
    password=$(cat "$dotfiles_password_file")
    return
  fi
  message="$1"
  if [[ -z "$message" ]]; then
    message="Enter password for data verification"
  fi
  stty -echo
  read -p "$message: " password; echo
  stty echo
}

function calculateChecksum() {
  pushd "$dotfiles_dir" > /dev/null
  checksum=$(find . ! -path "*/.git*" -type f ! -name ".DS_Store" ! -name ".dotfiles_sum" ! -name "README.md" -print0  | sort -dzf | xargs -0 shasum -b -a 256 | xargs -0 echo "$password" | shasum -b -a 256 | awk '{print $1}')
  popd > /dev/null
}

function updateChecksum() {
  calculateChecksum
  echo "$checksum" > "$dotfiles_checksum_file"
}

function getDotfilesChecksum() {
  dotfiles_checksum=$(cat "$dotfiles_checksum_file")
}

function verifyChecksum() {
  calculateChecksum
  getDotfilesChecksum
  if [[ "$checksum" != "$dotfiles_checksum" ]]; then
    echo "*** CHECKSUM VERIFICATION FAILED ***"
    exit 1
  fi
}

function require() {
  while (( "$#" )); do
    if [[ -z "$(which $1 2>/dev/null)" ]]; then
      echo "Please install $1"
      exit 1
    fi
    shift
  done
}

require git awk sort

if [[ -z "$(which shasum 2>/dev/null)" ]]; then
  if [[ -z "$(which sha256sum 2>/dev/null)" ]]; then
    echo "Please install shasum or sha256sum"
    exit 1
  fi
  alias shasum="sha256sum"
fi

run "$1" "$2"
